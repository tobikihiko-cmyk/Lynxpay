"use client";
import { useSearchParams } from "next/navigation";import { useEffect,useState } from "react";export default function Verify(){const token=useSearchParams().get("token");const[message,setMessage]=useState(token?"Verifying your email…":"Open the verification link sent to your work email.");useEffect(()=>{if(token)fetch("/api/lynxpay/auth/email-verification/confirm",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token})}).then(r=>setMessage(r.ok?"Email verified. Continue merchant onboarding.":"This verification link is invalid or expired."));},[token]);return <main className="grid min-h-screen place-items-center p-6"><section className="surface w-full max-w-md p-8"><p className="eyeline">EMAIL VERIFICATION</p><h1 className="mt-2 text-3xl font-bold">Protect the merchant owner account</h1><p role="status" className="mt-5 text-slate-600">{message}</p><a className="primary mt-6 inline-flex items-center" href="/onboarding">Continue</a></section></main>;}

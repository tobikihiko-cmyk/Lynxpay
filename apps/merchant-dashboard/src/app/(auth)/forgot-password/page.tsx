"use client";
import { useState } from "react";export default function Forgot(){const[email,setEmail]=useState("");const[done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();await fetch("/api/lynxpay/auth/password-reset/request",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setDone(true);}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="surface grid w-full max-w-md gap-5 p-8"><p className="eyeline">ACCOUNT RECOVERY</p><h1 className="text-3xl font-bold">Reset password</h1>{done?<p>Check your email for the secure reset link.</p>:<><label className="field">Work email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="primary">Send reset link</button></>}</form></main>;}

"use client";
import { useSearchParams } from "next/navigation";import { useState } from "react";export default function Reset(){const token=useSearchParams().get("token");const[password,setPassword]=useState("");const[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/lynxpay/auth/password-reset/confirm",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,new_password:password})});setMessage(r.ok?"Password reset. Sign in with your new password.":"The reset link is invalid or expired.");}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="surface grid w-full max-w-md gap-5 p-8"><p className="eyeline">SECURE RECOVERY</p><h1 className="text-3xl font-bold">Choose a new password</h1><label className="field">New password<input type="password" minLength={12} autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary">Reset password</button>{message&&<p role="status">{message}</p>}</form></main>;}

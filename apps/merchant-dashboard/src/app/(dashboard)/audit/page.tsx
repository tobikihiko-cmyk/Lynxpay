"use client";
import { useEffect,useState } from "react";import { api } from "@/lib/api";type Entry={id:string;action:string;entity_type:string;entity_id:string;created_at:string;metadata:unknown};export default function Audit(){const[items,setItems]=useState<Entry[]>([]);useEffect(()=>{api<{items:Entry[]}>("/audit-logs?limit=100").then(x=>setItems(x.items));},[]);return <section><p className="eyeline">IMMUTABLE EVIDENCE</p><h2 className="mt-2 text-4xl font-bold tracking-[-.05em]">Audit trail</h2><div className="surface mt-6 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="bg-slate-50 text-xs text-slate-500"><th className="p-4">Action</th><th>Entity</th><th>Evidence</th><th>Time</th></tr></thead><tbody>{items.map(x=><tr className="border-t border-slate-100" key={x.id}><td className="p-4 font-bold">{x.action}</td><td>{x.entity_type}<small className="block font-mono text-slate-400">{x.entity_id}</small></td><td><code className="text-xs">{JSON.stringify(x.metadata)}</code></td><td className="pr-4 text-xs text-slate-500">{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></section>;}
